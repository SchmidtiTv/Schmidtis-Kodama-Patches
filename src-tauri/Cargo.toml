[package]
name = "kodama"
version = "1.0.0"
description = "Kodama - YouTube Music Client"
authors = []
license = ""
edition = "2021"
rust-version = "1.77"

# Audio playback performs sample-rate conversion in the CoreAudio callback. The default
# unoptimized dev profile makes that real-time path consume a full CPU core while `tauri dev`
# is open. Keep debug symbols and assertions, but optimize the code developers exercise.
[profile.dev]
opt-level = 1

[features]
e2e = ["dep:tauri-plugin-wdio", "dep:tauri-plugin-wdio-webdriver"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri             = { version = "2", features = ["tray-icon", "image-png"] }
tauri-plugin-shell   = "2"
tauri-plugin-opener  = "2"
tauri-plugin-dialog  = "2"
tauri-plugin-fs      = "2"
tauri-plugin-http    = "2"
tauri-plugin-updater = "2"
tauri-plugin-process = "2"
tauri-plugin-single-instance = { version = "2", features = ["deep-link"] }
tauri-plugin-deep-link = "2"
tauri-plugin-wdio = { version = "1", optional = true }
tauri-plugin-wdio-webdriver = { version = "1", optional = true }
serde             = { version = "1", features = ["derive"] }
serde_json        = "1"
reqwest           = { version = "0.12", features = ["blocking", "json"] }
url               = "2"
tokio             = { version = "1", features = ["time"] }
discord-rich-presence = "1.1"
rodio      = { version = "0.20", features = ["symphonia-all"] }
symphonia  = { version = "0.5",  features = ["all"] }
rustfft    = "6"
ssstretch  = "0.1.0"
base64     = "0.22"
# OS media controls (Windows SMTC, macOS Now Playing, Linux MPRIS) + media keys.
souvlaki   = "0.7"

[target.'cfg(target_os = "macos")'.dependencies]
# Runtime Dock icon support for the app-icon personalization feature.
objc  = "0.2"
cocoa = "0.24"

[target.'cfg(windows)'.dependencies]
# xcap (bug-report screenshot) doesn't compile on macOS (xcap 0.0.14 macos backend) — Windows only.
xcap = "0.0.14"
windows = { version = "0.58", features = [
    "Win32_Media_Audio",
    "Win32_System_Com",
    "Win32_Foundation",
    "Win32_System_Threading",
    "Win32_Graphics_Dwm",
    "Win32_UI_Shell",
    "Win32_UI_WindowsAndMessaging",
] }
